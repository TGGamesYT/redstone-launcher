# Electron downloads its binary from a postinstall script, so installs must be
# allowed to run scripts — otherwise `npm i` leaves electron half-installed.
# A project .npmrc overrides a globally-set ignore-scripts=true.
ignore-scripts=false
